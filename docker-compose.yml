version: "3.9"

services:
  streamcat-monai:
    build: .
    container_name: streamcat-monai
    ports:
      - "8000:8000"
      - "8001:8001"
      - "8002:8002"
    environment:
      - CONFIG_PATH=config/config.yaml
    volumes:
      - ./:/app
    command: ["bash", "docker/entrypoint.sh"]
